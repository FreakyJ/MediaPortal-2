<?xml version="1.0" encoding="utf-8"?>
<Include
    xmlns="www.team-mediaportal.com/2008/mpf/directx"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Source="screens\master_dialog_bare.xaml"
    >
  <Include.Resources>
        <ControlTemplate x:Key="Contents_Template">
            <DockPanel
          Context="{Model Id=CC11183C-01A9-4F96-AF90-FAA046981006}" LastChildFill="False">
            <Label Content="Push a button on the input Device!" Color="{ThemeResource TextColor}" FontSize="36" DockPanel.Dock="Top" HorizontalAlignment="Center"/>
                <Label Content="{Binding AddKeyLabel}" Color="{ThemeResource TextColor}" FontSize="36" DockPanel.Dock="Top" HorizontalAlignment="Center"/>
                <Label Content="{Binding AddKeyCountdownLabel}" Color="{ThemeResource TextColor}" FontSize="36" DockPanel.Dock="Top" HorizontalAlignment="Center"/>
        </DockPanel>
        </ControlTemplate>
    </Include.Resources>
</Include>